{
  "name": "social-network-api",
  "version": "1.0.0",
  "description": "A social network API using Express, MongoDB, and TypeScript",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "node dist/seeds/index.js",
    "dev": "tsc && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.13.0",
    "mongoose": "^8.5.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/mongoose": "^5.11.96",
    "typescript": "^5.5.4"
  }
}
